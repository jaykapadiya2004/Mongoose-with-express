<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %></title>
</head>
<body>
    <h1><%= product.name %></h1>
    <ul>
        <li>
            price : <%= product.price %>
        </li>
        <li>
            category : <a href="/product?category=<%= product.category %>"><%= product.category %></a>
        </li>
    </ul>
    <a href="/product">All Products</a>
    <a href="/product/<%= product._id %>/edit">edit me </a>
    <form action="/product/<%= product._id %>?_method=DELETE" method="post">
        <button >delete</button>
    </form>
</body>
</html>